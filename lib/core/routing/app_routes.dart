/// Named route constants for [MaterialApp] navigation.
abstract final class AppRoutes {
  static const String home = '/';

  /// Expects a `Lesson` as route arguments.
  static const String lessonDetail = '/lesson-detail';

  /// Expects the lesson id (`String`) as route arguments.
  static const String quiz = '/quiz';
}
